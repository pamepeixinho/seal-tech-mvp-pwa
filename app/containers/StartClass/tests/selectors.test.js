// import { fromJS } from 'immutable';
// import { selectStartClassDomain } from '../selectors';

describe('selectStartClassDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toBe(false);
  });
});
